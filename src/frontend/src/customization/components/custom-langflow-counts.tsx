// White-label seam (FORK_CHANGES.md): the wrapped component is the upstream GitHub
// star-count / Discord member-count header widget - this platform has neither, so it
// renders nothing rather than linking out to the upstream project's community channels.
export function CustomLangflowCounts() {
  return null;
}

export default CustomLangflowCounts;
